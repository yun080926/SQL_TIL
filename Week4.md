# SQL_BASIC 4주차 정규 과제 

📌SQL_BASIC 정규과제는 매주 정해진 분량의 `초보자를 위한 BigQuery(SQL) 입문` 강의를 듣고 간단한 문제를 풀면서 학습하는 것입니다. 이번주는 아래의 **SQL_Basic_4th_TIL**에 나열된 분량을 수강하고 `학습 목표`에 맞게 공부하시면 됩니다.

**4주차 과제부터는 강의 내용을 정리하는 것과 함께, 프로그래머스에서 제공하는 SQL 문제를 직접 풀어보는 실습도 병행합니다.** 강의에서는 **배운 내용을 정리하고 주요 쿼리 예제를 정리**하며, 프로그래머스 문제는 **직접 풀어본 뒤 풀이 과정과 결과, 배운 점을 함께 기록**해주세요. 완성된 과제는 Github에 업로드하고, 링크를 스프레드시트 'SQL' 시트에 입력해 제출해주세요.

**👀(수행 인증샷은 필수입니다.)** 

## SQL_BASIC_4th

### 섹션 4. 쿼리 잘 작성하기, 쿼리 작성 템플릿 및 오류를 잘 디버깅하기

### 3-4. 오류를 잘 디버깅하는 방법



## 섹션 5. 데이터 탐색 - 변환

### 4-1. INTRO

### 4-2. 데이터 타입과 데이터 변환(CAST, SAFE_CAST)

### 4-3. 문자열 함수(CONCAT, SPLIT, REPLACE, TRIM, UPPER)

### 4-4. 날짜 및 시간 데이터 이해하기(1) (타임존, UTC, Millisecond, TIMESTAMP/DATETIME)



## 🏁 강의 수강 (Study Schedule)

| 주차  | 공부 범위              | 완료 여부 |
| ----- | ---------------------- | --------- |
| 1주차 | 섹션 **1-1** ~ **2-2** | ✅         |
| 2주차 | 섹션 **2-3** ~ **2-5** | ✅         |
| 3주차 | 섹션 **2-6** ~ **3-3** | ✅         |
| 4주차 | 섹션 **3-4** ~ **4-4** | ✅         |
| 5주차 | 섹션 **4-4** ~ **4-9** | 🍽️         |
| 6주차 | 섹션 **5-1** ~ **5-7** | 🍽️         |
| 7주차 | 섹션 **6-1** ~ **6-6** | 🍽️         |

<br>

<!-- 여기까진 그대로 둬 주세요-->

---

# 1️⃣ 개념정리

## 3-4. 오류를 디버깅하는 방법

~~~
✅ 학습 목표 :
* 오류의 정의에 대해 설명할 수 있다. 
* 오류 메시지를 보고 디버깅이라는 과정을 수행할 수 있다. 
~~~

### 오류(Error)란?

- 라틴어 *Errare*("방황하다, 길을 잃다")에서 유래한 단어
- **부정확하거나 잘못된 행동**을 의미하며, 실수와 동의어로 쓰이기도 함
- SQL에서는 쿼리를 작성하면서 실행이 안 되는 상황으로 나타남

### 오류 메시지가 알려주고자 하는 것

오류 메시지는 두 가지 역할을 한다.

1. **길잡이** — "현재 작성한 방식으로는 답을 얻을 수 없어요"
2. **문제 진단** — "이 부분에 문제가 있어요"

> 💡 오류가 발생하면 겁먹지 말고 **"길잡이가 더 좋은 길로 안내하려는 것"** 이라고 생각하자.  
> 오류 메시지를 읽고 → 해석하고 → 해결 방법을 찾아가는 과정이 **디버깅(Debugging)**이다.

### BigQuery의 대표 오류 유형 — Syntax Error (문법 오류)

- SQL 문법을 지키지 않아 발생하는 오류
- **디버깅 흐름**: 오류 발생 → 오류 메시지 확인 → 구글 검색 / 공식 문서 / ChatGPT에 질문

```
오류 발생
    ↓
오류 메시지와 함께 오류 발생
    ↓
오류 메시지 검색 (구글 / 공식 문서 / ChatGPT)
    ↓
해결 방법 적용
```

### ChatGPT로 디버깅하는 방법

쿼리와 오류 메시지를 함께 제공하면 효과적으로 도움을 받을 수 있다.

```
"BigQuery에서 [오류 메시지]라는 오류가 발생했어요.
쿼리는 다음과 같아요:
[쿼리 붙여넣기]
어떻게 해결할 수 있을까요?"
```

---

### 자주 만나는 BigQuery 오류 패턴 4가지

#### ① `SELECT list must not be empty at [10:1]`

```sql
-- ❌ 오류 쿼리
SELECT          -- SELECT와 FROM 사이에 컬럼이 없음
FROM table
WHERE ...
```

- **오류 해석**: SELECT 목록이 [10:1] 위치에서 비어 있으면 안 됩니다
- **원인**: `SELECT`와 `FROM` 사이에 조회할 컬럼을 명시하지 않음
- **해결**: SELECT 뒤에 원하는 컬럼명 또는 `*`을 작성

```sql
-- ✅ 수정 쿼리
SELECT column1, column2
FROM table
WHERE ...
```

---

#### ② `Number of arguments does not match for aggregate function COUNT`

```sql
-- ❌ 오류 쿼리
SELECT
  COUNT(id, kor_name)   -- COUNT 안에 인자를 2개 넣음
FROM basic.pokemon
```

- **오류 해석**: 집계 함수 COUNT의 인자 수가 일치하지 않습니다
- **원인**: `COUNT()`는 인자를 **1개만** 받는데 2개를 넣음
- **해결**: COUNT 안에 인자를 하나만 사용

```sql
-- ✅ 수정 쿼리
SELECT
  COUNT(id)
FROM basic.pokemon
```

---

#### ③ `SELECT list expression references column type1 which is neither grouped nor aggregated`

```sql
-- ❌ 오류 쿼리
SELECT
  type1,
  COUNT(id) AS cnt
FROM basic.pokemon
-- GROUP BY 없음
```

- **오류 해석**: SELECT 목록의 표현식이 그룹화되거나 집계되지 않은 열(type1)을 참조합니다
- **원인**: 집계 함수(`COUNT`)와 일반 컬럼(`type1`)을 함께 SELECT할 때 `GROUP BY`를 명시하지 않음
- **해결**: GROUP BY에 집계 기준 컬럼을 추가

```sql
-- ✅ 수정 쿼리
SELECT
  type1,
  COUNT(id) AS cnt
FROM basic.pokemon
GROUP BY type1
```

---

#### ④ `Syntax error: Expected end of input but got keyword SELECT`

```sql
-- ❌ 오류 쿼리
SELECT
  type1,
  COUNT(id) AS cnt
FROM basic.pokemon
GROUP BY
  type1

SELECT        -- 쿼리가 끝나지 않았는데 SELECT가 또 등장
  *
FROM basic.trainer
```

- **오류 해석**: 입력이 끝날 것으로 예상되었지만 SELECT 키워드가 입력되었습니다
- **원인**: 하나의 실행 단위 안에 SELECT가 2개 이상 존재. 이전 쿼리가 끝나지 않은 상태에서 새 SELECT가 시작됨
- **해결**: 쿼리 사이에 `;`(세미콜론)을 추가하거나, 실행할 쿼리만 드래그해서 선택 후 실행

```sql
-- ✅ 수정 쿼리
SELECT
  type1,
  COUNT(id) AS cnt
FROM basic.pokemon
GROUP BY type1;   -- 세미콜론으로 쿼리 구분

SELECT *
FROM basic.trainer;
```

---

#### ⑤ `Syntax error: Expected ")" but got end of script at [8:11]`

```sql
-- ❌ 오류 쿼리
SELECT
  name,
FROM (
  SELECT
    *
  FROM basic.trainer
  WHERE
    id = 3
-- 서브쿼리를 닫는 ) 가 없음
```

- **오류 해석**: ")"가 예상되지만 [8:11]에서 스크립트가 끝났습니다
- **원인**: 서브쿼리(괄호 안의 쿼리)를 열었지만 닫는 괄호 `)`를 작성하지 않음
- **해결**: 서브쿼리 끝에 닫는 괄호 추가

```sql
-- ✅ 수정 쿼리
SELECT
  name
FROM (
  SELECT *
  FROM basic.trainer
  WHERE id = 3
)  -- 닫는 괄호 추가
```

---

### 디버깅 핵심 마인드셋

| 상황 | 생각의 전환 |
|------|------------|
| 오류 발생 | ❌ "망했다" → ✅ "길잡이가 문제를 알려주는 것" |
| 오류 메시지 | ❌ 무시한다 → ✅ 번역/해석해서 원인 파악 |
| 해결 안 될 때 | ❌ 포기 → ✅ 구글·공식 문서·ChatGPT에 질문 |



## 4-2. 데이터 타입과 데이터 변환(CAST, SAFE_CAST)

~~~
✅ 학습 목표 :
* 데이터 타입의 종류를 설명할 수 있다. 
* 데이터 타입을 변환하는 방법을 설명할 수 있다. 
~~~

### 변환을 위한 함수

- `SELECT` 문에서 데이터를 변환시킬 수 있음
- `WHERE`의 조건문에도 사용 가능
- 데이터의 **타입**에 따라 다양한 함수가 존재

```sql
SELECT
  컬럼1,
  컬럼2,
  변환함수(컬럼3)   -- SELECT 절에서 변환
FROM 테이블
WHERE 조건문
GROUP BY 집계할 컬럼
```

---

### 데이터 타입의 종류

| 타입 | 예시 |
|------|------|
| 숫자 | 정수: `1`, `2`, `3` / 실수: `3.14` |
| 문자 | `"나"`, `"데이터"` |
| 시간, 날짜 | `2024-01-01`, `2024-04-01 28:59:10` |
| 부울(Bool) | `TRUE`, `FALSE` |
| 기타 | `JSON`, `ARRAY` 등으로 확장 가능 |

> 💡 주로 사용하는 기본 타입은 숫자·문자·시간·부울 4가지이며, 이후 더 확장된 타입도 존재한다.

---

### 데이터 타입이 중요한 이유 — 보이는 것과 저장된 것의 차이

화면에 보이는 값과 실제로 DB에 저장된 타입이 다를 수 있다.

| 보이는 값 | 실제 저장 타입 |
|-----------|---------------|
| 빈 값 | `""` (빈 문자열) 또는 `NULL` |
| `1` | 숫자 `1` 또는 문자 `"1"` |
| `2023-12-31` | `DATE` 타입 또는 문자 `"2023-12-31"` |

→ 내가 생각한 타입과 실제 저장 타입이 다를 경우, **타입을 변환**해야 원하는 연산을 할 수 있다.

---

### CAST — 데이터 타입 변환 함수

```sql
-- 기본 문법
CAST(값 또는 컬럼 AS 바꿀_타입)
```

**예시 1 — 숫자 → 문자 변환 (성공)**

```sql
SELECT
  CAST(1 AS STRING)   -- 숫자 1을 문자 "1"로 변환
```

**예시 2 — 문자 → 숫자 변환 (실패 → 오류 발생)**

```sql
SELECT
  CAST("카일스쿨" AS INT64)
-- "카일스쿨"은 숫자로 변환 자체가 불가능
-- → Bad int64 value 오류 발생
```

> ⚠️ 변환이 불가능한 값에 `CAST`를 쓰면 오류가 발생한다.

---

### SAFE_CAST — 안전한 타입 변환

변환에 실패하더라도 오류 대신 **NULL**을 반환한다.

```sql
-- 기본 문법
SAFE_CAST(값 또는 컬럼 AS 바꿀_타입)
```

```sql
SELECT
  SAFE_CAST("카일스쿨" AS INT64)
-- 변환 실패 → 오류 없이 NULL 반환
```

| 함수 | 변환 실패 시 |
|------|------------|
| `CAST` | ❌ 오류 발생 |
| `SAFE_CAST` | ✅ NULL 반환 (안전) |

> 💡 **SAFE_ 접두사**가 붙은 함수는 모두 실패 시 NULL을 반환한다. 데이터에 변환 불가 값이 섞여 있을 가능성이 있다면 `SAFE_CAST`를 쓰는 것이 안전하다.

---

### 수학 함수 — SAFE_DIVIDE

수학 함수에는 평균, 표준편차, 코사인 등 다양한 연산이 존재한다.

**나누기 시 주의할 점:**

```sql
-- ❌ x/y 직접 사용 시 y=0이면 zero error 발생
SELECT x / y FROM 테이블

-- ✅ SAFE_DIVIDE 사용 권장
SELECT SAFE_DIVIDE(x, y) FROM 테이블
-- x, y 중 하나라도 0이면 오류 대신 NULL 반환
```

> 💡 나누기 연산에서 분모가 0이 될 가능성이 있을 때는 항상 `SAFE_DIVIDE(x, y)`를 사용하자.



## 4-3. 문자열 함수(CONCAT, SPLIT, REPLACE, TRIM, UPPER)

~~~
✅ 학습 목표 :
* 문자열 함수들의 종류를 이해하고 어떠한 상황에서 사용하는지 설명할 수 있다. 
~~~

<!-- 새롭게 배운 내용을 자유롭게 정리해주세요.-->



## 4-4. 날짜 및 시간 데이터 이해하기(1) (타임존, UTC, Millisecond, TIMESTAMP/DATETIME)

~~~
✅ 학습 목표 :
* 날짜 및 시간 데이터 타입과 UTC의 개념을 설명할 수 있다. 
* DATE, DATETIME, TIMESTAMP 에 대해서 설명할 수 있다.
* 시간함수들의 종류와 시간의 차이를 추출하는 방법을 설명할 수 있다. 
~~~

<!-- 새롭게 배운 내용을 자유롭게 정리해주세요.-->


<br>

<br>

---

# 2️⃣ 확인문제 & 문제 인증

## 프로그래머스 문제 

> 조건에 맞는 도서 리스트 출력하기
>
> **먼저 문제를 풀고 난 이후에 확인 문제를 확인해주세요**
>
> 문제 링크 
>
> :  https://school.programmers.co.kr/learn/courses/30/lessons/144853

<!-- 문제를 풀기 위하여 로그인이  필요합니다. -->

<!-- 정답을 맞추게 되면, 정답입니다. 라는 칸이 생성되는데 이 부분을 캡처해서 이 주석을 지우시고 첨부해주시면 됩니다. --> 



## 문제 1

> **🧚Q. 프로그래머스 문제를 풀던 규서는 여러 번의 시행착오 끝에 결국 혼자 해결하기 어려워 오류 메시지를 공유하며 도움을 요청했습니다. 여러분들이 오류 메시지를 확인하고, 해당 SQL 쿼리에서 어떤 부분이 잘못되었는지 오류 메시지를 해석하고 찾아 설명해주세요.**

~~~sql
# 조건에 맞는 도서 리스트 출력하기
# 규서의 SQL 첫 번째 풀이
SELECT BOOK_ID, PUBLISHED_DATE
FROM BOOK
WHERE CATEGORY = '인문'
  AND YEAR(PUBLISHED_DATE, 2021);
  
오류 메시지 : Error: Number of arguments does not match for function YEAR
~~~



~~~
여기에 답을 작성해주세요!
~~~



### 🎉 수고하셨습니다.
