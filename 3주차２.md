# SQL_BASIC 3주차 정규 과제 

📌SQL_BASIC 정규과제는 매주 정해진 분량의 `초보자를 위한 BigQuery(SQL) 입문` 강의를 듣고 간단한 문제를 풀면서 학습하는 것입니다. 이번주는 아래의 **SQL_Basic_3rd_TIL**에 나열된 분량을 수강하고 `학습 목표`에 맞게 공부하시면 됩니다.

**3주차 과제는 문제 풀이를 중심으로**, 강의에서 제시된 예제 문제 중 **7 문제 이상을 선택하여 직접 풀어본 뒤**, 강의 영상의 풀이와 비교해 **틀린 부분, 맞은 부분, 새롭게 배운 개념**을 구체적으로 정리해주세요. (적어도 3문제는 정리해야 합니다.) 완성된 과제는 Gihub에 업로드하고, 링크를 스프레드시트 'SQL' 시트에 입력해 제출해주세요.

**👀(수행 인증샷은 필수입니다.)** 

## SQL_BASIC_3rd

### 섹션 3. 데이터 탐색 - 조건, 추출, 요약

### 2-6. 연습문제 1~3번

### 2-6. 연습문제 7~9번

### 2-6. 연습문제 10~12번

### 2-6. 연습문제 13~17번

### 2-7. 정리 

### 2-8. 새로운 집계함수



## 섹션 4. 쿼리 잘 작성하기, 쿼리 작성 템플릿 및 오류를 잘 디버깅하기

### 3-1. INTRO

### 3-2. SQL 쿼리 작성하는 흐름

### 3-3. 쿼리 작성 템플릿과 생산성 도구 



## 🏁 강의 수강 (Study Schedule)

| 주차  | 공부 범위              | 완료 여부 |
| ----- | ---------------------- | --------- |
| 1주차 | 섹션 **1-1** ~ **2-2** | ✅         |
| 2주차 | 섹션 **2-3** ~ **2-5** | ✅         |
| 3주차 | 섹션 **2-6** ~ **3-3** | ✅         |
| 4주차 | 섹션 **3-4** ~ **4-4** | 🍽️         |
| 5주차 | 섹션 **4-4** ~ **4-9** | 🍽️         |
| 6주차 | 섹션 **5-1** ~ **5-7** | 🍽️         |
| 7주차 | 섹션 **6-1** ~ **6-6** | 🍽️         |

<br>

---

# 1️⃣ 개념정리

## 2-6. 연습문제

~~~
✅ 학습 목표 :
* 연습문제(7문제 이상) 푼 것들 정리하기
~~~

### 📌 데이터 탐색 - 조건, 추출, 요약 정리

강의에서 배운 데이터 탐색의 전체 흐름은 다음과 같다:

- **조건(필터링)** → `WHERE` / `HAVING`
- **추출** → `SELECT`
- **변환** → `SELECT` 내 함수 사용
- **요약(집계)** → `GROUP BY` + 집계함수 (`AVG`, `COUNT`, `SUM`, `MAX`, `MIN`)

---

### 문제 1번

> **각 트레이너(trainer_id)별로 보유한 포켓몬의 수를 구하라.**

```sql
-- 내가 쓴 쿼리
SELECT trainer_id, COUNT(pokemon_id) AS pokemon_count
FROM trainer_pokemon
GROUP BY trainer_id;
```

- ✅ **맞은 부분**: `GROUP BY trainer_id`와 `COUNT` 사용 정확
- 새롭게 배운 것: `COUNT(*)`와 `COUNT(컬럼명)`의 차이 — `COUNT(*)`는 NULL 포함, `COUNT(컬럼명)`은 NULL 제외

---

### 문제 2번

> **포켓몬 중 attack이 100 이상인 포켓몬의 이름과 공격력을 출력하라.**

```sql
-- 내가 쓴 쿼리
SELECT name, attack
FROM pokemon
WHERE attack >= 100;
```

- ✅ **맞은 부분**: `WHERE` 조건 정확히 사용
- ✅ 정답과 동일

---

### 문제 3번

> **각 type1별 포켓몬의 수를 구하라.**

```sql
-- 내가 쓴 쿼리
SELECT type1, COUNT(*) AS count
FROM pokemon
GROUP BY type1;
```

- ✅ **맞은 부분**: `GROUP BY type1` 정확
- 새롭게 배운 것: 집계 시 `SELECT`에는 `GROUP BY`에 쓴 컬럼 또는 집계함수만 올 수 있음

---

### 문제 7번

> **각 type1별 평균 HP를 구하고, 평균 HP가 50 이상인 타입만 출력하라.**

```sql
-- 내가 쓴 쿼리
SELECT type1, AVG(hp) AS avg_hp
FROM pokemon
GROUP BY type1
HAVING AVG(hp) >= 50;
```

- ✅ **맞은 부분**: 집계 후 조건은 `HAVING` 사용 — `WHERE`가 아님!
- ⚠️ **주의**: `WHERE`는 집계 전 개별 행 필터링, `HAVING`은 집계 후 그룹 필터링

---

### 문제 8번

> **포켓몬의 최대 공격력(attack)과 최소 공격력을 구하라.**

```sql
-- 내가 쓴 쿼리
SELECT MAX(attack) AS max_attack, MIN(attack) AS min_attack
FROM pokemon;
```

- ✅ **맞은 부분**: `MAX`, `MIN` 함수 사용 정확
- ✅ 정답과 동일

---

### 문제 9번

> **각 트레이너(trainer_id)별 보유 포켓몬의 평균 레벨을 구하라.**

```sql
-- 내가 쓴 쿼리
SELECT trainer_id, AVG(level) AS avg_level
FROM trainer_pokemon
GROUP BY trainer_id;
```

- ✅ **맞은 부분**: `AVG` + `GROUP BY` 조합 정확
- 새롭게 배운 것: 평균값은 소수점이 나올 수 있으므로 `ROUND(AVG(level), 1)`처럼 반올림 처리도 가능

---

### 문제 10번

> **포켓몬 중 type2가 NULL인 포켓몬의 수를 구하라.**

```sql
-- 내가 쓴 쿼리
SELECT COUNT(*) AS no_type2_count
FROM pokemon
WHERE type2 IS NULL;
```

- ✅ **맞은 부분**: NULL 조건은 `= NULL`이 아니라 `IS NULL` 사용
- ⚠️ **틀리기 쉬운 부분**: `WHERE type2 = NULL`은 작동하지 않음

---

### 문제 11번

> **각 type1별로 포켓몬 수와 평균 공격력을 함께 출력하라.**

```sql
-- 내가 쓴 쿼리
SELECT type1, COUNT(*) AS count, AVG(attack) AS avg_attack
FROM pokemon
GROUP BY type1;
```

- ✅ **맞은 부분**: `SELECT`에 여러 집계함수 동시 사용 가능
- ✅ 정답과 동일

---

### 문제 12번

> **배틀에서 승리(winner_id)한 횟수가 3회 이상인 트레이너를 출력하라.**

```sql
-- 내가 쓴 쿼리
SELECT winner_id, COUNT(*) AS win_count
FROM battle
GROUP BY winner_id
HAVING COUNT(*) >= 3;
```

- ✅ **맞은 부분**: `HAVING COUNT(*) >= 3` 정확히 사용
- 새롭게 배운 것: `HAVING`절에서도 집계함수를 다시 써야 함 (alias 사용 불가)

---

## 2-8. 새로운 집계함수

~~~
✅ 학습 목표 :
* SQL 쿼리 구조를 이해할 수 있다. 
* SELECT, FROM, WHERE을 활용하는 방법을 설명할 수 있다. 
~~~

### GROUP BY ALL

BigQuery에서 2024년 2월 26일 이후 새롭게 사용 가능해진 기능으로, `SELECT`에 있는 **모든 비집계 컬럼을 자동으로 GROUP BY**에 포함시켜준다.

```sql
-- 기존 방식
SELECT
  FirstName AS first_name,
  LastName AS last_name,
  SUM(PointsScored) AS total_points
FROM PlayerStats
GROUP BY
  first_name,
  last_name;

-- GROUP BY ALL 사용 (새로운 방식)
SELECT
  FirstName AS first_name,
  LastName AS last_name,
  SUM(PointsScored) AS total_points
FROM PlayerStats
GROUP BY ALL;
```

**장점**: `SELECT`에 컬럼을 추가해도 `GROUP BY`를 수정할 필요가 없어 편리하다.

**결과 예시**:

| first_name | last_name | total_points |
|------------|-----------|--------------|
| Noam       | Adams     | 7            |
| Jie        | Buchanan   | 13           |
| Kiran      | Coolidge   | 1            |

---

## 3-2. 쿼리를 작성하는 흐름

~~~
✅ 학습 목표 :
* 쿼리를 작성하는 흐름을 설명할 수 있다.
~~~

쿼리를 잘 작성하려면 단순히 SQL 문법을 아는 것을 넘어, **체계적인 사고 흐름**이 필요하다.

### 쿼리 작성 6단계 흐름

| 단계 | 내용 |
|------|------|
| ① 지표 고민 | 어떤 문제를 해결하기 위해 데이터가 필요한가? |
| ② 지표 구체화 | 추상적이지 않고 구체적인 지표 명시 (분자, 분모 표시) |
| ③ 지표 탐색 | 유사한 문제를 해결한 케이스가 있나 확인 → 있으면 해당 쿼리 리뷰 |
| ④ 쿼리 작성 | 데이터가 있는 테이블 찾기 (ERD 활용) → 1개면 바로 작성, 2개 이상이면 JOIN 고민 |
| ⑤ 데이터 정합성 확인 | 예상한 결과와 동일한지 확인 |
| ⑥ 쿼리 가독성 & 저장 | 나중을 위해 깔끔하게 작성하고, 쿼리는 재사용되므로 문서로 저장 |

> 💡 **핵심 포인트**: 지표 이름은 정의, 기이름(계산 방법)이 구체적이어야 한다. 테이블이 2개 이상이면 JOIN이 필요하다.

---

## 3-3. 쿼리 작성 템플릿과 생산성 도구

~~~
✅ 학습 목표 :
* 생산성 도구를 만들 수 있다.
~~~

### 쿼리 작성 템플릿

쿼리를 작성하기 전, 아래 항목을 먼저 주석으로 정리하면 쿼리 작성이 훨씬 수월해진다.

```sql
# 쿼리를 작성하는 목표, 확인할 지표 :
# 쿼리 계산 방법 :
# 데이터의 기간 :
# 사용할 테이블 :
# Join KEY :
# 데이터 특징 :

SELECT

FROM
WHERE
```

---

### 생산성 도구 : Espanso

템플릿을 매번 직접 입력하는 것을 잊지 않기 위해 **Espanso**라는 텍스트 자동 치환 도구를 활용한다.

- **공식 사이트**: https://espanso.org/
- 윈도우10, Mac, Linux 모두 무료로 사용 가능
- 특정 단어(trigger)를 입력하면 원하는 문장(템플릿)으로 자동 변경됨

#### Espanso 핵심 로직

> 특정 단어가 감지되면 정의된 것으로 바꾼다!

```yaml
# Match 구조
- trigger: ":date"
  replace: "October 11, 2021"
```

#### Espanso 설정 방법 (base.yml)

1. 터미널에서 `espanso path`로 설정 파일 경로 확인
   - Mac: `/Users/user/Library/Application Support/espanso/match`
   - Windows: `C:\Users\user\AppData\Roaming\espanso/match`
2. 터미널에서 `espanso edit` 입력 → `base.yml` 파일 바로 열림
3. 아래 내용을 복사해 붙여넣기(공백 포함):

```yaml
- trigger: ":sql"
  replace: |
        # 쿼리를 작성하는 목표, 확인할 지표 :
        # 쿼리 계산 방법 :
        # 데이터의 기간 :
        # 사용할 테이블 :
        # Join KEY :
        # 데이터 특징 :

        SELECT

        FROM
        WHERE
```

4. 저장 후 Espanso 아이콘에서 **Reload Config** 클릭

#### BigQuery에서 확인

BigQuery 콘솔에서 `:sql`을 입력하면 아래처럼 템플릿이 자동 완성된다:

```sql
# 쿼리를 작성하는 목표, 확인할 지표 :
# 쿼리 계산 방법 :
# 데이터의 기간 :
# 사용할 테이블:
# Join KEY :
# 데이터 특징 :

SELECT

FROM basic.pokemon
WHERE
```

> 💡 앞으로 쿼리 작성은 **trigger 단어 입력 → 테이블 FROM 채우기**로 시작!

<br>
<br>

---

# 2️⃣ 학습 인증란

> 강의 수강 및 BigQuery 실습 완료 (캡처 첨부)
> - 섹션 2-6 연습문제 풀이 완료
> - 섹션 2-7 정리 완료
> - 섹션 2-8 GROUP BY ALL 실습 완료
> - 섹션 3-1 ~ 3-3 수강 완료
> - Espanso 설치 및 `:sql` 트리거 설정 완료, BigQuery 콘솔에서 동작 확인

<br><br>

---

# 3️⃣ 확인문제

## 문제 1

> **🧚Q. 포켓몬 연구에 흥미를 느낀 혜인은 각 타입(type1)별 평균 공격력(attack)을 비교해보고 싶었습니다.**

~~~
조건 : attack이 50 이상인 포켓몬만 포함
보고 싶은 컬럼 : type1
집계 내용 : 각 type1 별 평균 공격력
정렬 기준 : 평균 공격력을 기준으로 내림차순 정렬
~~~

```sql
SELECT type1, (ㄱ)
FROM pokemon
(ㄴ) attack >= 50
(ㄷ) type1
ORDER BY (ㄱ) (ㄹ);
```

~~~
답:
ㄱ = AVG(attack) AS avg_attack
ㄴ = WHERE
ㄷ = GROUP BY
ㄹ = DESC

최종 쿼리:
SELECT type1, AVG(attack) AS avg_attack
FROM pokemon
WHERE attack >= 50
GROUP BY type1
ORDER BY AVG(attack) DESC;
~~~

**풀이 설명**:
- `ㄱ`: 평균 공격력을 구하므로 `AVG(attack) AS avg_attack`
- `ㄴ`: 집계 이전에 개별 행을 필터링해야 하므로 `WHERE`
- `ㄷ`: type1별로 묶어야 하므로 `GROUP BY`
- `ㄹ`: 내림차순 정렬이므로 `DESC`

### 🎉 수고하셨습니다.
